#include "main.h"

/**
 * main - print s _putchar, followed by a new line
 * Return: Always 0 (success)
 */
int main(void)
{
	char task[10] = "_putchar";
	int i = 0

		for (i = 0, i < 8; i++)
		{
			_ptchar(task[i]);
		}
	_putchar('\n');
	return (0);
}
